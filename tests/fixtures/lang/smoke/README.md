Smoke `.lang` fixtures used for quick compiler/runtime checks.

- `test_add.lang`: minimal arithmetic flow.
- `test_strict_simple.lang`: minimal strict-safe flow.

These are intentionally tiny and live outside repo root to keep top-level clean.
