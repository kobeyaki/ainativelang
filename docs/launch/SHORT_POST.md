AINL `v1.1.0` is out as the first public GitHub release baseline.

This release is about **trust and explicit boundaries**: compiler/runtime/grammar ownership is clearer, strict vs non-strict artifacts are machine-classified and CI-checked, and the contributor path is much cleaner from README through release docs.

Compatibility paths remain documented and intentional. No hidden semantic widening was introduced just to make the release look cleaner.
