# Memory — 2026-04-06

## Session Notes (early AM, ~3:38 AM EDT)

### Context: Yaki (@YeBuddy42069) Feedback on QTs
- Yaki reviewed two recent @ainativelang quote tweets and flagged account quality as the key variable
- The two QTs shown were GOOD examples — both featured verified/notable accounts actively in AI discourse:
  - @ArchiveExplorer (verified, K badge) — ranting about Claude token waste
  - @bcherny (Boris Cherny, verified) — news about Claude policy changes
- Both QTs had tight, technically credible copy; one standout line: *"you're not paying a rate limit. you're paying a model to re-read its own homework 30 times."*
- Yaki's standard: **only QT verified or high-follower accounts actively in AI discourse where AINL has a natural, technically credible entry point**

### Action Items
- Tighten auto-engage bot QT targeting criteria:
  - Add follower floor filter before QT fires
  - Require verification status or high-signal account
  - Add relevance scoring before pulling QT trigger
  - Currently casting too wide — needs guardrails
- Kobe to confirm whether to update auto-engage.js with these guardrails

### X API Note
- X API Basic ($100/month) is the only real unlock for cold replies and quote tweets at scale
- Current automation running on free tier
- Worth upgrading if account growth continues

### Seedream Status
- Was failing earlier in the session (image generation)
- Status unknown at time of flush — pending check

---

## Session — Mid-Morning (4:33 AM – 11:37 AM EDT)

### Model Cost Optimization
- Switched all cron/isolated agents to `anthropic/claude-haiku-4-5` (default)
- Pinned `main` agent to `anthropic/claude-sonnet-4-6` (direct chat stays sharp)
- Config patched via `gateway config.patch`, gateway restarted at ~4:37 AM EDT
- Estimated 70-80% cost reduction on background job spend

### hourly-post.js Upgraded to v2.0
- Full rewrite committed to ainl-x repo (commit: `c7cbc5c`)
- Changes:
  1. **Engagement feedback loop** — fetches real Twitter metrics (likes/RTs/replies/impressions) for last 10 tweets, feeds top performers back as style signal
  2. **gpt-4o-mini** instead of gpt-4o (~10x cheaper, same quality for 280-char tweets)
  3. **Semantic similarity via embeddings** — each posted tweet embedded + stored; cosine similarity check (threshold 0.88) before posting; forces regen if too similar
  4. **Better context sources** — added arxiv AI + arxiv ML RSS feeds alongside HN
  5. **Thread support** — 20% chance during peak hours, max 2/week, min 48h between, 3-tweet technical threads
- New state files: `post-embeddings.json`, `engagement-scores.json`, `thread-cooldown.json`
- Model for tweets: `gpt-4o-mini` via OpenAI API directly (not Anthropic)

### Content Quality Issues Flagged
- Multiple off-brand posts slipping through v2 retry logic:
  - `2041128183756357730`: gerund opener ("Building AINL means...") — banned by prompt
  - `2041158335735410871`: community welcome pattern ("To those quietly building with us...") + "future-proof" + vague inspiration — multiple violations
- `community` category guidance is producing soft inspirational content instead of sharp one-liners
- **Action needed:** Tighten `community` category guidance in hourly-post.js; possibly block it temporarily

### AINL Daily Briefing Cron — Error Fixed
- Job `25fbac64` was erroring due to Telegram delivery failure: `@heartbeat` chat ID couldn't be resolved
- Fix applied: `--best-effort-deliver` flag set so delivery failure doesn't fail the job
- Job ran at ~9:00 AM EDT; execution succeeded, delivery still failing but non-fatal
- Underlying issue: `@heartbeat` Telegram chat ID misconfigured in delivery.to

### Cron Job Health (as of 10:30 AM EDT)
- 13/14 jobs: OK
- 1 job error: AINL Daily Briefing (delivery only, execution fine)
- No consecutiveErrors on any other jobs

### Usefulcoin.cash Domain Issue (Yaki)
- Site `usefulcoin.cash` was down — ICANN contact verification never completed
- Netlify site `usefulcoin.netlify.app` was live the entire time (Netlify token expired but site still served)
- Yaki completed email verification via Namecheap — both `usefulcoin.cash` and `plushify.wtf` verified
- Domain reactivation expected within 24-48h (typically faster)
- Netlify token `nfp_NL5Zga4QFvDWUq68j4te2N1RpSyz6ThC2de7` is expired/revoked — needs refresh if Netlify API access needed again

---

## Daily Briefing — 9:00 AM EDT

**AINL KING // Monday April 6, 2026**

---

### 1. X PERFORMANCE (Last 24h)

Active posting pipeline is running. Last 5 confirmed published tweets (real tweetIds, timestamped):
- *"AINL 1.4.0 release: seamless ArmaraOS integration"* — 04/04, 9:44 PM
- *"AINL runs 17 deterministic graphs..."* — 04/04, 11:44 PM
- *"Compile once, deploy everywhere. 7.2x cheaper..."* — 04/05, 1:44 AM
- *"AINL ensures zero runtime type errors. Compile-time validates..."* — 04/05, 3:44 AM
- *"Google's Gemma 4 may be open, but the control plane isn't..."* — 04/05, 5:44 AM

**Signal:** The Gemma 4 reactive post (tying a trending model drop to AINL's core thesis) is the sharpest angle in recent batches — news hooks > pure principle posts. Short-form posts ("Compile once. The decision is made. The graph controls.") are cleaner than the long-form repetitive ones that have been looping. Volume is high (~hourly) which risks dilution. Quality variance is wide.

**Concern:** Too much repetition on the "7.2x cheaper / 17 graphs / zero runtime errors" triplet. Starting to feel like a loop, not a narrative.

---

### 2. COMMUNITY PULSE

- **GitHub Issues:** Zero open issues. No PRs. No community activity.
- **Commits:** Rate-limited on fetch — check manually if commit cadence matters this week.
- **Public chatter:** Web search unavailable (Brave API key not configured). Blind on external sentiment.

**Action needed:** Configure Brave API key (`openclaw configure --section web`) to restore market intelligence in future briefings.

---

### 3. MARKET INTELLIGENCE

Web search offline this session. Based on known landscape:
- **LangChain/LangGraph** continues to be the reference for agent orchestration, but carries real complexity debt — AINL's "we built the compiler" angle is still valid and under-exploited externally.
- **AI infrastructure funding** has been accelerating in early 2026 — positioning AINL as infrastructure (not a tool) is the right frame for that conversation.
- **Deterministic execution** is a contrarian position while the market chases reasoning models. That's a feature, not a bug — contrarian stances age well when the hype cycle turns.

---

### 4. PRODUCT ANGLES TO PUSH THIS WEEK

1. **The compiler vs. the graph** — "LangChain is a graph without a compiler. We built the compiler." This is your sharpest competitive line. Use it more in engagement, not just posts.
2. **Cost reality, not just cost claims** — $29.10/mo in production is a concrete number. Put it in context: what does equivalent LangGraph infra cost? Build that comparison publicly.

---

### 5. TODAY'S TOP 3 ACTIONS

1. **Fix the QT guardrails** — Yaki's feedback from this morning is actionable. Update `auto-engage.js` with follower floor + verification filter before the next engagement cycle runs. Wide casting is burning signal.
2. **Restore web search** — Configure Brave API key. Every briefing without it is flying partially blind on competitive intel.
3. **Refresh the content queue** — The "17 graphs / 7.2x / zero errors" triplet has saturated. Inject new angles: the compiler framing, a cost comparison post, or a reactive thread on the next major model release.

**⚠️ Alert:** X API is still on free tier. If engagement volume keeps climbing, the $100/mo Basic upgrade becomes a hard requirement — not optional. Evaluate this week.

---

## AINL Intelligence Digest — 12:00 PM EDT (16:00 UTC, Monday April 6)

### Execution Status
- **Supervisor Graph:** ✅ Compiled at 2026-04-06T12:00:04.719940Z
- **Intelligence Digest Module:** ⚠️ Blocked — AINL runtime venv missing dependencies (`requests`, etc.)
- **Web News Monitoring:** ❌ Blocked — Brave Search API key not configured in this session
- **TikTok Activity Monitor:** ❌ Not integrated — no TikTok API credentials configured

### Results
- **Mentions Found:** 0 (web search unavailable)
- **TikTok Activity (24h):** null (module not integrated)
- **Spike Detected:** false (baseline comparison blocked)
- **Memory Records Created:** 0 (pending API key configuration)

### Blocked Integrations
1. **Web News** — To enable: `openclaw configure --section web` (Brave Search API key required)
2. **TikTok** — Requires: TikTok API credentials + module configuration
3. **AINL Runtime** — Requires: Python dependencies in venv (requests, etc.)

### Next Scheduled Run
- **Time:** 2026-04-06 18:00 UTC (6:00 PM EDT)
- **Schedule:** 3x daily (08:00, 12:00, 18:00 UTC) per cron spec

### Action Items
1. Configure Brave API key to restore web news intelligence pipeline
2. Install missing AINL runtime dependencies (requests library)
3. Verify TikTok module if social signal tracking is required for AINL

## Arch — Character Locked (13:34 EDT)
- **Arch is LOCKED** — final canonical character confirmed by Yaki
- Black background, orange starfish, 5 arms, menacing glowing blue eyes, smirk — exact design
- Locked files:
  - `/data/.openclaw/workspace/ainl-video/arch/arch-LOCKED-canonical.jpg` — original
  - `/data/.openclaw/workspace/ainl-video/arch/arch-LOCKED-nobg.png` — rembg cutout (transparent bg)
- **Rules going forward:**
  - NEVER send Arch to MJ image-to-image again — he always drifts
  - All new scenes = generate background with MJ, composite Arch on top with PIL
  - Do NOT regenerate or replace the locked files
  - catbox URL: https://files.catbox.moe/pk9c8y.jpg (current upload)
