# Minimal meta-supervisor sample for OpenClaw bridge docs — status log only (no QueuePut / no extra scheduling).
include "modules/openclaw/cron_supervisor.ainl"

L0:
  R svc crm ->st
  R core concat "openclaw_full_unification status crm=" st ->line
  R openclaw_memory append_today line ->_
  Ret st
