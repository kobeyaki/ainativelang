# Agent Result Reader Demo (extension/OpenClaw, advanced)
# Purpose: read a single AgentTaskResult envelope identified by task_id via
# `agent.read_result`. This is a local-only, file-backed example.
#
# Advanced / operator-only:
# - advisory-only; assumes an external orchestrator wrote the result
# - not a safe default pattern for unsupervised agents or auto-remediation

S agent

L0: R agent read_result "demo-openclaw-monitor-001" ->result J result
